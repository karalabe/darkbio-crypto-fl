name: darkbio_crypto
description: "Post-quantum cryptography in Flutter, backed by Rust FFI"
version: 0.18.0
homepage: https://github.com/dark-bio/crypto-fl
repository: https://github.com/dark-bio/crypto-fl
issue_tracker: https://github.com/dark-bio/crypto-fl/issues

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: '>=3.35.0'

dependencies:
  cbor: ^6.5.1
  flutter:
    sdk: flutter
  flutter_rust_bridge: 2.13.0  # Pinned: frb_prefixed_binding.dart relies on internal APIs
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  ffi: ^2.1.4
  ffigen: ^19.1.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        ffiPlugin: true
      ios:
        ffiPlugin: true
      linux:
        ffiPlugin: true
      macos:
        ffiPlugin: true
      windows:
        ffiPlugin: true
